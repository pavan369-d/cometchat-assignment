{
  "name": "backend",
  "version": "1.0.0",
  "description": "job applies tracker app",
  "license": "ISC",
  "author": "pavan",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --env-file=.env index.js",
    "dev": "node --watch --trace-warnings --env-file=.env index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
  
    "cors": "^2.8.5",
   
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
 
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.9.3"
   
  }
}
